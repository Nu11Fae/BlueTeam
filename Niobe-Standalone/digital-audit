#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ENTRYPOINT="${ROOT_DIR}/scripts/digital-audit"

if [[ ! -x "${ENTRYPOINT}" ]]; then
  chmod +x "${ENTRYPOINT}" 2>/dev/null || true
fi

exec "${ENTRYPOINT}" "$@"
